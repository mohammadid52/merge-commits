import { useState, useEffect, useContext } from 'react';

const timeAndDate = () => {


    useEffect(() => {

        return () => console.log('Time and Date -> ', '');
    }, []);

    return {  };
};

export default timeAndDate;

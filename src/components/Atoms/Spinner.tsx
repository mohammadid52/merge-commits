import React from 'react';
import {FaSpinner} from 'react-icons/fa';

const Spinner = () => {
  return (
    <FaSpinner size="1.2rem" className="relative w-auto mr-4 animate-spin theme-text" />
  );
};

export default Spinner;
